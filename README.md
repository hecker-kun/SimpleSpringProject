# This is a minimal Spring application.

### Application launch

To run the application just enter the command  
```./gradlew clean build && java -jar build / libs / gs-rest-service-0.1.0.jar```  
In PowerShell, use the separator ";" instead of "&&"